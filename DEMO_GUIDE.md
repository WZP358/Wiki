## 开场白

​    我做的是企业内部知识库 Wiki 系统，技术栈是 Spring Boot 3、Spring Data JPA、MySQL 8、Redis、Vue3 用户端和 Vue2 若依管理后台。系统实现了用户登录注册、普通用户和管理员权限区分、Swagger 接口文档、文档树形目录、Markdown 编辑、版本历史、版本对比、版本回滚、全文检索、文档权限，以及 Redis 缓存和编辑锁。

然后打开：

`http://localhost:8080/swagger-ui.html`

说：

​    这里是后端 Swagger 接口文档，能看到认证、知识库、文档、评论、收藏、分享、后台管理等接口，说明后端 API 是按模块分层提供的。

## 注册



## 登录

管理员：

- 账号：`admin`
- 密码：`Admin@123456`

用户：

- 账号： `testuser`
- 密码： `123456`
- 账号： `newuser`
- 密码： `123456`



## 展示管理后台

打开：

`http://localhost:5184/login`

登录：

- 用户名：`admin`
- 密码：`Admin@123456`

### 1. 展示用户管理

进入：

`系统管理 -> 用户管理`

操作：

1. 展示左侧组织机构树。
2. 展示用户列表。
3. 指出每个账号有用户昵称、演示密码、部门、手机号、状态和创建时间。
4. 点击不同组织机构，例如“产品中心”“后端子团队”“客服中心”，展示按部门筛选用户。

可以这样说：

> 后台复用了若依管理系统的布局和组件。这里可以看到组织机构树和用户列表，账号都补充了见名知意的昵称、部门、手机号和演示密码，方便管理员进行用户分配和演示。系统至少区分管理员和普通用户，管理员可以进入后台管理，普通用户只能使用知识库业务功能。

### 2. 展示团队管理

进入：

`系统管理 -> 团队管理`

操作：

1. 展示组织树结构。
2. 展示团队包含：产品中心、前端子团队、后端子团队、测试与质量子团队、运维与部署子团队、客服中心、权限与用户任务组等。

可以这样说：

> 企业内部知识库通常要按部门或团队划分权限，所以系统内置了团队结构。后续文档权限里的“部门可见”就是基于这些团队关系判断的。

### 3. 展示 Wiki 管理

进入：

`Wiki 管理`

重点展示：

- 知识库管理
- 文档管理
- 评论/收藏/分享等管理页面
- 操作日志或审计页面

可以这样说：

> 管理后台主要用于管理员做全局治理，包括查看知识库、文档、用户、日志和业务数据。这样普通用户负责内容协作，管理员负责平台治理。

## 展示用户端知识库功能

打开：

`http://localhost:5173/login`

使用普通账号登录：

- 账号：`testuser`
- 密码：`123456`

### 1. 展示首页和知识库列表

登录后先停留在首页。

操作：

1. 展示左侧知识库列表。
2. 指出知识库类型：公开知识库、团队知识库、私有知识库。
3. 点击“公司制度与入职指南”。

可以这样说：

> 用户端是普通员工使用的知识库入口。左侧按知识库展示，知识库分为公开、团队和私有三类。公开知识库面向全员，团队知识库面向对应部门，私有知识库只对创建者或被邀请成员可见。

### 2. 展示树形目录

进入知识库后，展示文档目录侧栏。

操作：

1. 点击“公司制度与入职指南”。
2. 展示右侧文档主页。
3. 展示文档目录侧栏。
4. 点击目录里的“知识库权限说明”或“新员工入职流程”。
5. 点击目录侧栏的“收起/展开”按钮。

可以这样说：

> 文档采用树形目录管理，适合企业内部按章节组织制度、流程和技术文档。目录侧栏可以收起和展开，展开时右侧内容会自适应压缩，不会遮挡正文。

### 3. 展示 Markdown 编辑器

进入某篇文档后，点击编辑或进入编辑页。

推荐文档：

- `知识库权限说明`
- `需求评审模板`
- `接口响应结构规范`

操作：

1. 打开文档。
2. 进入编辑页面。
3. 展示左侧 Markdown 输入区域。
4. 展示右侧 HTML 预览区域。
5. 修改一行内容，例如加一句：

```markdown
本条内容用于课堂演示 Markdown 编辑和版本保存。
```

6. 点击保存或发布。

可以这样说：

> 文档编辑使用 Markdown，左侧编辑、右侧实时预览。保存时后端会把 Markdown 渲染成 HTML，并缓存已发布 HTML 内容，提高后续访问速度。

## 展示版本历史、对比和回滚

在编辑页中打开“版本历史”。

操作：

1. 点击版本历史。
2. 选择两个版本进行对比。
3. 展示新增、删除或变更行。
4. 点击“回滚到此版本”。
5. 回滚后再次打开文档，展示内容恢复。

可以这样说：

> 每次创建、修改和回滚文档，系统都会保存一条版本快照。版本历史支持对比两个版本，也支持回滚。回滚本身也会生成一个新版本，保证操作可追溯。

对应评分点：

- 版本历史
- 版本对比
- 版本回滚
- 日志和可追溯性

## 展示全文检索

进入：

左侧菜单或顶部入口中的“搜索”

操作：

1. 搜索关键词：`权限`
2. 展示搜索结果。
3. 搜索关键词：`发布`
4. 展示文档标题和内容命中。
5. 点击搜索结果进入文档。

可以这样说：

> 全文检索这里使用数据库 LIKE 实现，题目允许 Elasticsearch 或数据库 LIKE。系统会按知识库范围搜索标题和正文，并且只返回当前用户有权限查看的文档。

## 展示文档权限

这个部分非常重要，建议一定展示。

### 1. 展示公开知识库

### 2. 展示团队知识库

### 3. 展示私有知识库

## 展示 Redis 缓存应用

这一部分是评分 30%，必须讲清楚。

### 1. 热门 / 最新文档缓存

进入任意知识库主页，例如：

`公司制度与入职指南`

操作：

1. 展示“最近更新文档”。
2. 展示“热门文档”。
3. 打开某篇文档几次，使阅读量增加。
4. 返回知识库主页，看热门文档变化。

可以这样说：

> 最近更新和最多阅读文档列表会缓存到 Redis。对应 key 类似 `docs:latest:{kbId}` 和 `docs:hot:{kbId}`。当文档新增、修改、删除、回滚或阅读量变化时，系统会删除或刷新相关缓存，保证缓存一致性。

### 2. HTML 内容缓存

打开任意已发布文档。

可以这样说：

> 文档保存时，后端会把 Markdown 渲染成 HTML。已发布文档的 HTML 会缓存到 Redis，key 类似 `doc:html:{docId}`。用户再次访问文档时优先读取缓存，避免每次都重新渲染 Markdown。

### 3. 用户编辑锁

这个可以用两个浏览器或一个普通窗口加一个无痕窗口展示。

准备两个账号：

- 窗口 A：`product_manager`
- 窗口 B：`frontend_lead`

操作：

1. 窗口 A 打开同一篇可编辑文档，进入编辑。
2. 窗口 B 也打开同一篇文档，尝试进入编辑。
3. 窗口 B 会看到文档正在被其他用户编辑，不能覆盖保存。
4. 窗口 A 退出编辑或释放锁。
5. 窗口 B 再尝试编辑。

可以这样说：

> 编辑锁使用 Redis 实现，key 类似 `editing:{docId}`。用户开始编辑时使用 Redis 的 set-if-absent 设置锁，并带 TTL，防止多人同时编辑产生覆盖。用户保存或离开页面时释放锁，如果异常退出，也会依靠 TTL 自动过期。

## 展示评论、收藏和分享

打开一篇文档。

操作：

1. 在文档底部添加评论。
2. 点击收藏。
3. 点击分享生成分享链接。
4. 复制分享链接，用未登录窗口打开。

可以这样说：

> 除了核心 Wiki 功能，系统还补充了评论、收藏和分享，方便企业内部知识传播。分享访问也会检查文档是否可公开访问。

## 展示代码质量和架构

可以打开 IDE 或简单说明，不建议展示太久。

可以这样说：

> 后端按 Controller、Service、Repository、DTO 分层。统一异常通过业务异常和全局异常处理返回。关键操作会写操作日志。权限判断在后端 Service 层完成，避免只靠前端控制。Redis 缓存的 key 设计集中在文档服务中，文档修改、删除、回滚时会统一失效缓存。

可以点到以下包：

- `com.wiki.app.user`：用户登录注册
- `com.wiki.app.kb`：知识库和成员权限
- `com.wiki.app.doc`：文档、版本、搜索、缓存、编辑锁
- `com.wiki.app.admin`：后台管理
- `com.wiki.app.common`：统一响应、异常、错误码

## 收尾总结

最后可以这样总结：

> 这个系统围绕企业内部知识沉淀场景，实现了知识库、文档树、Markdown 编辑、版本管理、搜索、权限控制和后台管理。Redis 主要用于热门/最新文档缓存、文档 HTML 缓存和编辑锁，既提升访问性能，也解决多人编辑冲突。前端分为用户端和管理后台，用户端负责日常知识协作，后台负责用户、团队和内容治理。

## 十二、如果老师问问题

### 问：为什么不用 Elasticsearch？

答：

> 题目允许 Elasticsearch 或数据库 LIKE。考虑课程项目部署复杂度，我这里采用 MySQL LIKE 实现标题和正文检索，同时预留了搜索服务接口，后续可以替换为 Elasticsearch。

### 问：Redis 缓存一致性怎么保证？

答：

> 文档新增、修改、删除、回滚会删除最新/热门/搜索相关缓存，并重新写入 HTML 缓存。阅读量变化会清理热门文档缓存。编辑锁设置 TTL，异常退出后也能自动释放。

### 问：权限是在前端做还是后端做？

答：

> 后端做。前端只负责隐藏或禁用按钮，真正的查看、编辑、评论、收藏、分享都会在后端校验当前用户是否有权限。

### 问：版本回滚会不会覆盖历史？

答：

> 不会。回滚是把某个历史版本内容恢复为当前内容，并生成一个新的版本记录，因此历史链路仍然保留。

### 问：多人同时编辑怎么处理？

答：

> 进入编辑时会抢 Redis 编辑锁。拿到锁的人可以编辑，其他人只能查看锁占用提示。锁有 TTL，用户离开页面或保存后释放。





