## 前端环境变量示例（Vite）
#
# 开发环境通常不需要配置：默认请求走 /api，并由 vite proxy 转发到后端 8080。
# 如果你的前端不是通过 Vite dev server 运行（例如用 nginx/静态托管），请配置后端地址：
#
#   VITE_API_BASE_URL=http://localhost:8080/api
#
# 说明：
# - 必须以 /api 结尾（因为前端接口写的是 /kbs、/docs 等相对 api path）
# - 末尾多余的 / 会被自动去掉

VITE_API_BASE_URL=http://localhost:8080/api

